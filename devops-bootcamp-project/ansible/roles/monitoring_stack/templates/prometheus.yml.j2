global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "web-node"
    static_configs:
      - targets:
          # Web server private IP from requirements: 10.0.0.5
          - "10.0.0.5:{{ web_node_exporter_port }}"

